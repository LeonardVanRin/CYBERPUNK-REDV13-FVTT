{
  "type": "object",
  "properties": {
    "installLocation": {
      "type": "string",
      "enum": [
        "mall",
        "clinic",
        "hospital",
        "notApplicable"
      ]
    },
    "size": {
      "type": "integer",
      "minimum": 0
    }
  },
  "required": [
    "size"
  ]
}
